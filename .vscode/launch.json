{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug mlpl",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/main.go",
            "cwd": "${workspaceFolder}"
        }
    ]
}